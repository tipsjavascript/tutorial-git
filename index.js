const dogClass = (name) => {
    return name;
}

// B add class Cat

const catClass = (name) => {
    
    return name;
}


// C add class Tiger

const tigerClass = (name) => {
    
    return name;
}



module.exports = {
    dogClass,
    catClass,
    tigerClass
}